# TES 电-热微分方程与能量提取 — 信息汇总

目的:波形纵轴是**电流** I(t),不是功率。∫I dt 是电荷、不是能量。要得到事件能量,
必须用 TES 电路的微分方程把 I(t) 换算成功率 P(t) 再积分。
collection efficiency = E_abs / 10.37 keV。

参考文件(本目录):
- `Transition Edge Sensor Irwin.pdf` — Irwin & Hilton, *Transition-Edge Sensors*,
  Topics in Applied Physics 99 (2005) 63–150。第 72 页是小信号公式汇总表,
  第 74–75 页是电路图与两条微分方程,第 87–88 页是 ETF 能量推导(式 54–58)。
- `Collection Efficiency Analysis_....pdf` — CDMS wiki 导出(CUTE tower, R37 v4.0.0),
  第 2 页给出目前在用的两个近似公式 Method 1 / Method 2 及流程。

---

## 1. 电路(Irwin Fig. 2/3,与实际配置逐项吻合)

偏置电流 `Ib`(= QETbias)进入后分两路:一路经 **Rsh**(shunt),另一路经寄生电阻
**Rp** → **R_TES** → 电感 **L**。Thevenin 等效:

    V (= Vb) = Ib · Rsh
    R_L      = Rp + Rsh
    R0       = R_TES(正常态) / 2        (percentRn = 50)
    I0       = Vb / (R_L + R0)

### 常数来源(已确认)

全部在处理文件的 `detectorConfigDir/detectorConfigZip{N}` 里,**逐通道实测值**。
12 个通道全部验证过自洽性 Ib·Rsh/(R_L+R0) = |i0|,偏差 ≤ 0.2%;`driverGain`=8、
`variableGain`=−6、`rshunt`=5.00 mΩ 全通道一致;`r0/rn` 在 0.45–0.55 之间
(**并非严格 0.5**,应直接用记录的 r0 而不是 rn/2):

| 量 | 分支名 | PBS1 | PES2 |
|---|---|---|---|
| Ib (QETbias) | `qetBias` | 229.038 µA | 202.023 µA |
| Rsh | `rshunt` | 5.000 mΩ | 5.000 mΩ |
| Rp | `rp` | 14.240 mΩ | 13.645 mΩ |
| R_TES 正常态 | `rn` | 88.356 mΩ | 77.258 mΩ |
| R0 | `r0` | 44.159 mΩ | 38.935 mΩ |
| I0 | `i0` | **−18.069 µA** | −17.545 µA |
| P0 | `p0` | 1.4417e−11 W | 1.1984e−11 W |
| percentRn | `percentRn` | 50 | 50 |
| dt | `timePerBin` | 1.6 µs | 1.6 µs |
| 采样点数 | `binsPerTrace` | 32768 | 32768 |

**自洽性验证(全部通过)**
- R_L = Rp + Rsh = 19.240 mΩ
- Vb = Ib·Rsh = 1.1452 µV;事件级 RQ `PBS1vb` = −1.14519 µV(符号见 §4)
- Vb/(R_L+R0) = 18.063 µA = 配置里的 |i0| ✓
- I0²R0 = 1.442e−11 W = 配置里的 p0 ✓

## 2. ADC → 安培(已确认,精确)

增益链(`cdmsbats_config/UserSettings/BatRootSettings/analysis/configSNOLAB.R4.UMN.Addison`):

    DigitizerBinsPerVolt = 8192
    driverGain           = 8      (detectorConfig 逐通道)
    LPGain               = 4
    FBgain               = 12000  (= 2.4 匝比 × 5000 Ω 反馈电阻)

    ADC per amp = 8192 × 8 × 4 × 12000 = 3.145728e9
    1 ADC       = 3.1789e−10 A

**两重确认**:(1) 事件级 RQ `{chan}gain` = `{chan}norm` = **3.145728e9**,11 个通道
全部一致、无逐事件变化;(2) §6d 用它复现官方 Eabs 到 0.2%。
`variableGain = −6` **不参与**这个换算(gain RQ 里没有它)。

用法:δI(t) = (ADC(t) − 基线ADC) / 3.145728e9;I0 取配置值(不要用基线的绝对值,
SQUID 锁定点是任意的)。实测基线 28720 ADC 与 RQ `PBS1bs` = 28715.5 一致。

## 3. 两条微分方程(Irwin 式 3、4,第 75 页)

    热:  C dT/dt = −P_bath + P_J + P          (P = 信号功率,我们要 ∫P dt)
    电:  L dI/dt = V − I·R_L − I·R_TES(T, I)

Irwin 第 72 页汇总表给出小信号解:能量脉冲的电流响应
δI(t) ∝ e^(−t/τ₊) − e^(−t/τ₋),**这正是我们双指数模板的理论出处**;
τ_el = L/(R_L + R0(1+β_I)) 是电学时间常数,τ = C/G 是热学时间常数
(对应演讲稿 Q&A 里"上升 = L/R 电学、下降 = C/G 热学"的说法)。

## 4. 符号约定(已查清)

配置里 `i0` 与事件级 `vb` **都是负值**,而原始波形的脉冲是**向上**的。
自洽的解释:电流与电压取负号约定,脉冲使 |I| 减小 → 有符号的 I 增大 →
ADC 上升。因此计算时必须用**带符号的** I0 = −18.069 µA、Vb = I0(R_L+R0) = −1.1456 µV,
配合 δI = (ADC−基线)/gain > 0。用正的 I0 会得到负能量。

## 5. 严格的能量表达式(本文档的推导)

对热方程整脉冲积分(脉冲完全衰减 ⇒ ∫C dT = 0):

    E_abs = ∫P dt = −∫δP_J dt + ∫ΔP_bath dt

用电学方程精确展开焦耳功率 P_J = I·V_TES,V_TES = V − I·R_L − L dI/dt:

    δP_J = V·δI − R_L(2 I0 δI + δI²) − L·I·dδI/dt

整脉冲积分时电感项 L[I0 δI + δI²/2] 的端点值为零(Irwin 式 57 同结论),于是

    −∫δP_J dt = I0(R_L − R0) ∫δI dt + R_L ∫δI² dt          ……(★)

★ 的线性系数 I0(R_L−R0) 与 **Irwin 式 (54)** ΔP_ETF = −I0(R0−R_L)δI 完全一致。

### 与现有三个公式的对照

| 公式 | 线性项系数 | 二次项 | 结论 |
|---|---|---|---|
| **★ 严格式** | I0(R_L−R0) | **+R_L** | 基准 |
| CollEff **Method 1**(Noah) | (2R_L/(R_L+R0) − 1)·Vb = I0(R_L−R0) ✓ | **+2R_L** | 线性项正确,二次项大 2 倍 |
| CollEff **Method 2**(Watkins) | Vb − 2I0R_L = −I0(R_L−R0) | **−R_L** | 整体差一个负号(ΔI 定义相反),二次项符号也反 |
| Irwin 式 (58) | I0R_L − V = −I0R0 | +R_L | 定义不同:它算的是 −∫V_TES δI dt,不是 −∫δP_J dt |

数值验证(PBS1,单事件):★ 与 M1 相差约 1–2%(二次项占比小),M2 = −★。

## 6. 三级修正(待做)

1. **一级**:用 ★ 的二次项 +R_L(纠正 M1 的 2 倍、M2 的符号)。
2. **二级(积分窗截断)**:实际积分在衰减 90% 处截断,电感边界项
   L[I0 δI + δI²/2] 不再为零,应从数据端点直接算出补上。**需要 L**(见 §7)。
3. **三级(bath 泄漏)**:∫ΔP_bath dt = G∫ΔT dt。ΔT 可由 R_TES(t) 反演:
   R_TES(t) = V_TES(t)/I(t),V_TES(t) = V − I·R_L − **L·dI/dt**(Irwin 式 56,
   电感项常被忽略),再经 R(T) 关系(α_I)换成 ΔT。**需要 L、α_I、G**(见 §7)。

## 6b. dI/dV:PDF 里给出的完整算法(Irwin 第 82–83 页)

dI/dV 的频域形式就是**复阻抗** Z(ω) = V_ω / I_ω。做法:在偏置线上加白噪声/正弦
激励,测各频率下的电流响应,得到 Z(ω) 的实部-虚部轨迹(Irwin Fig. 4 是实测例子,
呈半圆形),然后拟合

    Z(ω) = R_L + iωL + Z_TES(ω)                                    (式 41)
    Z_TES(ω) = R0(1+β_I) + [R0·ℒ_I/(1−ℒ_I)]·(2+β_I)/(1+iω τ_I)      (式 42)

**这个拟合直接给出 β_I、ℒ_I、τ_I 和 L**;再用式 (18)、(14) 可解出 **C**;
由 ℒ_I ≡ P_J0 α_I/(G T0) 解出 **α_I**;由 τ = C/G 解出 **G**。
即:一次 dI/dV 测量 → 二级修正需要的 L、三级修正需要的 α_I、C、G 全部到手。

矩阵形式(式 40,"广义响应矩阵"):

    [ 1/τ_el + iω        ℒ_I G / (I0 L) ] [ I_ω ]   [ V_ω / L ]
    [ −I0R0(2+β_I)/C     1/τ_I + iω     ] [ T_ω ] = [    0    ]

低频强反馈极限 s_I(0) = −1/[I0(R0−R_L)](式 39),与严格式 ★ 的线性系数
I0(R_L−R0) 同源(互为倒数与符号),再次交叉验证 ★ 正确。

处理链侧:`configSNOLAB.R4.UMN.Addison` 有 `DIDV_FIT_POLES = 3`、
`DIDV_FIT_LOWPASS_CUTOFF = 10000`、`SAVE_DIDV_RQ = 1`,说明 BatRoot 支持这套拟合,
只是这批处理没填出结果。

## 6c. CollEff 文档给出的口径(第 1、6、8 页)

- **定义**:η_total = E_abs / E_true,E_abs 是**全部通道之和**。0 V 时无 Luke 放大;
  加偏压时需扣除 Luke 项 (e·V/ε,Ge 的 ε ≈ 3 eV)。
- **实测量级**(R37 CUTE tower):10.37 keV 事件的 TES 总吸收能量 **2.7–3.0 keV**,
  收集效率 **26.2%**(Z1 0V)、**29.2%**(Z1 50V)、**40.9/41.2%**(Z3)、**26.3%**(Z6),
  均由高斯拟合峰位给出。
- **我们 Z7 的一致性检查**:RQ `PBS1Eabs` 在 K 线事件上中位 **263.5 eV**,
  11 通道求和 ≈ 2.9 keV → η ≈ 28%,与上表同量级,佐证 `Eabs` 单位是 **eV**、
  定义与 CollEff 研究一致。
- **Rp 来源**:文档说明寄生电阻由**专门的 Midas Tool 定标文件**提取
  (如 `TES_231211_1234.root`),每探测器一份 —— 这正是 Rp 数值分歧的根源,
  应以定标/detectorConfig 的实测值为准,而非标称值。

## 6d. 官方 Eabs 的完整定义(已逆向确认,复现到 0.2%)

从 `configSNOLAB.R4.UMN.Addison` 挖出的精确口径:

| 项 | 参数 | 值 |
|---|---|---|
| 触发位置 | `PBS1triggerTime` = 26.2128 ms ÷ 1.6 µs | **bin 16383**(注意:不是我们内部用的 16050) |
| 基线窗 | `P_BASELINE_MIN_FROM_START_USEC`=150,`P_BASELINE_MAX_TO_TRIG_USEC`=1000 | bin 93 … 15758,取**均值** |
| 积分窗 | `P_INTEGRAL_MIN_TO_TRIG_USEC`=500,`P_INTEGRAL_MAX_FROM_TRIG_USEC`=1000 | bin 16071 … 17008 |
| 滤波 | `P_INTEGRAL_BUTTERWORTH_ORDER`=5,`CUTOFF`=20 kHz,`DO_FILTER`=1 | 积分前先滤波 |

用这套定义 + 严格式 ★ 复现单事件 `PBS1Eabs`:

    Eabs(RQ)   我的★     比值
      298.30   297.70   1.0020
      239.56   239.11   1.0019
      328.72   328.06   1.0020
      296.36   295.36   1.0034
      ...      ...      中位 1.0019

**结论:官方 Eabs 就是 ★ 式(或与之差 0.5% 的 M1),线性系数 I0(R_L−R0) 得到确认。**
副产品:`INTtrunc` = ΣδI(安培,按 bin 求和、不乘 dt);比值 626413 ≈ 1/dt。

**这同时解决了 Rp 之争**:用 detectorConfig 的 rp = 14.24 mΩ 才能复现到 0.2%;
若 Rp = 1 mΩ 线性系数会大 53%,若 17 mΩ 会小 11% —— 都会立刻破坏这个一致性。
故**处理链使用且应当使用 detectorConfig 的逐通道实测 rp**。

## 6e. 积分窗截断误差(实测量化,80 个 K 线事件)

把积分窗从触发后 1000 µs 逐步放宽:

| 触发后窗口 | 中位 E_abs [eV] | 相对 1000 µs |
|---|---|---|
| 1000 µs(官方) | 268.7 | 1.000 |
| 2000 µs | 283.4 | 1.055 |
| 4000 µs | 293.7 | 1.093 |
| 8000 µs | 300.3 | **1.118** |
| 16000 µs | 301.7 | 1.123 |

**官方窗口漏掉了约 12% 的能量**,8 ms 后基本收敛。这是"算不准"的**首要来源**,
比二次项系数(0.5%)大一个量级以上。注意窗口放宽也会引入更多低频噪声/基线漂移,
所以正确做法不是无脑放宽,而是:窗口取到收敛处 + 补上电感边界项 L[I0δI+δI²/2]
(需要 L)+ 扣除 bath 泄漏(需要 α_I、G)。

## 6f. 复盘发现:暴力放宽窗口在噪声通道上不成立(重要修正)

逐通道对比官方 Eabs 与不同积分窗的严格式 E₁(series 063934,~150 个 K 线事件,中位):

| 通道 | official | E₁@1ms | @4ms | @16ms | 行为 |
|---|---|---|---|---|---|
| PAS1 | 269.2 | 267.3 | 296.4 | 311.7 | 单调收敛 +16% |
| PBS1 | 278.1 | 277.6 | 297.7 | 305.6 | 单调收敛 +10% |
| PES1/PFS1/PDS1/PBS2 | … | ≈official | +6~8% | +7~14% | 单调收敛 |
| PCS1 | 256.2 | 254.5 | 281.5 | **269.0** | 非单调 |
| PAS2 | 257.7 | 258.0 | 265.6 | **242.5** | 掉到 official 以下 |
| PCS2 | 254.9 | 255.3 | 249.4 | **237.0** | 随窗变差 |
| PDS2 | 255.3 | 252.2 | 248.2 | **337.3** | 低频伪影主导,乱跳 |

两个结论:
1. **@1ms 列与官方 Eabs 全部 11 通道逐一吻合(≤1%)** —— 严格式和全套常数得到
   迄今最强的验证;
2. **"把窗口放宽收回 12%"只对安静通道成立**。噪声通道(尤其 PDS2 的低频伪影)
   在长窗口里积分的是漂移噪声,估计量不收敛甚至变差。
   → 正确做法:**中等窗口积数据 + 解析补尾巴**。尾巴有两种等价来源:
   (a) 边界项公式(需 L、τ);(b) **用我们每事件的 2-exp 拟合曲线外推尾巴**
   (拟合尾巴的积分有解析式,对漂移免疫)——(b) 不需要任何缺失参数,可立即实施。

事件级求和的确定数值(同一 series,158 事件,中位):
- 官方 Eabs 求和:3243.6 eV → **收集效率 31.28%**
- E₁@16ms 求和:3288.9 eV → 31.72%(但含上表噪声通道的失真,不作为最终值)
- 修正后的最终值应由"中窗 + 拟合尾巴外推"给出(预期 ≈33–34%,待算)。

另:收集效率分母若探测器加了偏压需含 Luke 项;Z7 本轮的 HV 偏压值尚未落实
(infoDir/processingTree 或 ODB 中查,或问导师)——这是分母侧唯一悬而未决的量。

## 6g. raw trace 版已作废(保留其中仍然有效的诊断)

第一版 `plot_current_power_overlay.py` 对 **raw trace** 做功率换算和积分,
老师明确否掉:必须用拟合脉冲(见 §6h)。该脚本与其输出**已从仓库删除**,
避免和正确结果混在一起(git 历史里仍可查)。以下是它留下的、仍然成立的诊断:

**累积曲线的漂移**:raw 版的累积积分在触发后 1–2 ms 爬到平台,之后**不保持水平,
而是上下漂 ±20%**(ev 210571 从 259 eV 一路掉到 34 eV,ev 190030 270→146 eV)。
全 11 通道同一事件的漂移形状**完全同相**(5 ms 处同时下凹、7.5–12 ms 处同时上凸,
PDS2 最大 80→570 eV)⇒ **共模基线摆动**,不是各通道独立噪声。两点结论:
1. 这是 §6f "暴力放宽窗口不收敛"的直接证据;
2. 共模扣除(用全通道平均漂移做基线校正)有可能救回长窗 raw 积分 —— 但 §6h 之后
   这条路已无必要:拟合脉冲根本不需要长窗。

**下降段的定量归因(已算清)**:累积曲线下降 ⇔ P<0 ⇔ δI 低于所减的基线。全部由
**微小基线偏移**解释,不是新物理:

| 事件(PBS1) | 2–16 ms 平均 δI | 该偏移贡献的能量 | 实测累积下降 |
|---|---|---|---|
| 210571 | **−5.83 nA** | **−229 eV** | 259→34 eV = −225 eV ✓ |
| 190030 | −3.16 nA | −124 eV | 270→146 eV = −124 eV ✓ |
| 30646 | +0.89 nA | +35 eV | 300→345 eV = +45 eV ✓ |

灵敏度:**1 nA 的基线误差 × 15 ms = 42 eV**(= 267 eV 信号的 16%)。而这些偏移只有
**~1σ**(前触发噪声 RMS 4.7–6.2 nA),即完全正常的低频摆动 —— 问题不在数据异常,
而在于**长窗把直流误差乘以窗长,却不增加信号**(信号只有 32 fW × 1.5 ms)。

共模程度(事件 30646,11 通道,触发后 2–16 ms):通道间平均相关 **0.55**,共模模板
解释各通道方差的 **18–85%**(PAS2 85%、PDS2 80%、PBS1 仅 26%)。频率:14 ms 片段的
FFT 分辨率本身就是 71 Hz,最强 bin 落在第一格,故只能说漂移**慢于约 70 Hz**,
真实频率需用整条 52 ms 波形或 NoisePSD 才能定 —— 勿引用"71 Hz"为谱线。

结论(官方积分窗):
- PBS1 中位 **267.2 eV**(Method 1),与官方 Eabs 中位 ~268 eV 一致;
- 事件 30646 全通道求和 **3397 eV = 32.8%**(16 ms 窗 4012 eV = 38.7%,含
  PDS2/PAS2 低频漂移,不可信,见 §6f);
- 二次项占官方窗能量的 **0.9–2.8%**(Method 1 的 +2R_L);三系数对照(15 事件中位):
  method1 267.19 eV / **exact 265.66 eV (0.9943)** / method2 262.60 eV (0.9828)。
  即 M1 比精确式高 **0.57%**,M2 低 **1.15%**,与 §5 的解析预期一致。

## 6h. 用**拟合脉冲**积分(老师指定的做法)—— 并修正 §6e/§6f 的结论

`scripts/plot_fitted_current_power.py` → `results/plots/current_power_overlay/`
(4 张 PNG + 1 个数值表,已覆盖并取代 §6g 的 raw 版):

- `..._current_power_{chan}_15events.png` / `..._current_power_allchan_ev30646.png`
  — 电流脉冲与功率脉冲**画在同一张图**:**左纵轴 δI(µA)**、**右纵轴 P(fW)**,
  两轴按线性系数 I0(R_L−R0) 绑定(老师指定的画法)。灰色 raw 只作参照。
- `..._cumulative_energy_*.png` — 累积能量,横轴 0 → 52.43 ms(整条 trace)。
- `..._energies.txt` — 逐事件/逐通道能量表 + 逐事件收集效率。
老师的意见:积分必须用**拟合后的电流脉冲**,不是 raw trace,这样没有噪声影响;
积分范围 0 → 52.43 ms(整条 trace)。

做法:把双指数模型**直接在安培量纲上重拟合**(lp_fit_align 的 checkpoint 里 `amp`
是"各自 trace 全局峰值"归一化后的值,没存那个峰值,无法还原成电流,所以必须重拟合)。
5 个参数全自由,pretrigger 自由于 16383±3000。**关键实现坑**:直接用 A / s / bins
拟合会失败(amp~1e-7 与 pretrigger~1.6e4 量纲差 11 个数量级,curve_fit 第一步就
"收敛",τ 原地不动等于初值)。必须换算成 nA / ms / 相对触发的 t0 再拟合。

**闭式积分**(拟合脉冲 δI = A[e^(−t/τ_f) − e^(−t/τ_r)]):

    ∫δI dt  = A(τ_f − τ_r)
    ∫δI² dt = A²[τ_f/2 + τ_r/2 − 2τ_fτ_r/(τ_f+τ_r)]
    ⇒ E = I0(R_L−R0)·A(τ_f−τ_r) + c₂R_L·A²[…]      (无窗口依赖)

数值积分(0→52.43 ms)与闭式**逐事件完全相等**,公式得到验证。

**结果(PBS1,15 个 K 线事件,拟合质量 NRMSE 0.03–0.07,τ_r 69–164 µs,τ_f 166–271 µs)**:

| 量 | 中位值 |
|---|---|
| E(闭式=数值,全 trace) | **273.0 eV** |
| E(拟合脉冲,只积官方窗) | 268.5 eV |
| E(raw,官方窗) | 267.2 eV |
| E(raw,整条 trace) | 281.7 eV(不可信,漂移主导) |

**⚠ 修正 §6e/§6f**:拟合脉冲的能量有 **96–99%(中位 99%)落在官方积分窗内**。
所以 §6e 里"官方窗漏掉约 12%"的结论**是错的**——那 12% 来自基线漂移被长窗放大
(§6g 已定量:1 nA × 15 ms = 42 eV),**不是真实的脉冲尾巴**。真实尾巴只有约 1%。
这也解释了为什么"放宽窗口"在噪声通道上不收敛:根本没有尾巴可收。

**收集效率(11 通道求和 ÷ 10.37 keV,15 个事件)**:

- 拟合脉冲:中位 **32.97%**(散布 3.9 个百分点)
- 官方窗 raw:中位 32.76%(散布 3.6)

两者只差 0.2 个百分点 ⇒ **收集效率 ≈ 33%**,此前"预期 33–34%"的猜测成立,
但理由不同(不是补回尾巴,而是本来就没漏多少)。异常事件:ev 240632
in-window 只有 0.845、η 达 45%,疑似 pileup 或拟合不良,需单独看。

## 7. 缺失信息

- **dIdV 拟合参数全部为空**:事件级 RQ `L`、`l`(loop gain)、`beta`、`tau0`、
  `taum`、`taup`、`p0`、`r0`、`rp`、`rsh`、`i0` 在这批处理里**全是哨兵值 −999999**,
  即 dIdV 拟合没有填。因此目前**没有** L、β_I、ℒ_I、α_I、C、G。
  算法本身已完全清楚(见 §6b),缺的是**输入数据**。已确认 MSI 上**没有**这些数据:
  `R4/MidasTools/` 目录为空,全盘搜索 `TES_*.root`(CollEff 文档所用的定标文件
  命名)**无结果**,`R4/Raw/` 的 34 个 series 也没有 dIdV 专用采数。
  → 需要:dIdV/IV 定标数据的位置,或让这批处理把 DIDV RQ 跑出来。
- ~~`Eabs` RQ 对不上~~ **已解决**:见 §6d,用官方触发位/基线窗/积分窗复现到 0.2%。
- **`collectionEfficiency` RQ 全为 0**(未填),`energyResolution` 同。
- **Rp 数值**:口述 1 mΩ(损坏后 17 mΩ),detectorConfig 逐通道实测 12.4–14.6 mΩ
  (PFS2 = 7.25)。§6d 的 0.2% 复现**证明处理链用的是 detectorConfig 实测值**,
  分析上应沿用;仅当认为该实测值本身有误时才需要另议。

## 8. 下一步

1. 确认 Rp 用哪个值;
2. 拿到 dI/dV(或其拟合参数 L、β_I、ℒ_I、τ),补齐二级、三级修正;
3. 确认 `Eabs` 的积分窗/基线定义,复现现有 RQ 后再逐级改进;
4. 逐通道算 E_abs,求和后除以 10.37 keV 得 collection efficiency,与现有方法对比。
